# discovery-service/src/main/resources/application.yml
server:
  port: 8761   # fallback nếu chưa kéo được config

spring:
  application:
    name: discovery-service
  config:
    import: optional:configserver:http://config-service:8888
  cloud:
    config:
      username: configuser
      password: configpass

